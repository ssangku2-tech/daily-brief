# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## What this is

"My Agent" — a single-page PWA (no build step, no framework, no package.json) that shows a Korean user one screen of daily briefing: overnight US market moves, news for their watchlist stocks, Anthropic news, and their own schedule, mail, and (later) assets. It is served as static files (GitHub Pages). Everything the app needs lives in three files: `index.html` (markup + CSS + JS in one file), `sw.js` (service worker), `manifest.json` (PWA manifest).

The app is at v0.3. The US market card (관심 종목 + 주요 뉴스 + 앤트로픽) exists; the 자산 card is a link-out placeholder.

**Everything the user reads refreshes at access time (as of 2026-08-07); the daily files are the fallback.** Prices, news, and unread mail are all fetched client-side on every load/refresh; `briefings/` and `agenda/` are what renders when those fetches fail or the user is offline. The one thing that *cannot* move client-side is anything Claude writes — the briefing's `summary`, each headline's `ko` line, and the mail-importance summaries — because a client-side LLM call would bill tokens on every app open. Those stay in the daily files. **Translation, however, is not an LLM job here** — see the 제목 번역 note below: headlines are Koreanized at access time through Google Translate's free public endpoint, so a story that arrived minutes ago still reads in Korean.
- **Prices**: `index.html` calls the Cloudflare Worker directly and overwrites the briefing file's price/changePct. The Worker sends `Access-Control-Allow-Origin: *`, so the browser can call it with no proxy.
- **News** (`loadNews`): also through that Worker, which turned out to have a **generic `?url=<encoded>` proxy** and a **`?news=<ticker>` endpoint** (both used by the portfolio app) alongside `?symbol=`. Both send `Access-Control-Allow-Origin: *`. This reversed the old "news can't be client-side" conclusion — that was true of *Google News RSS specifically* (no CORS headers, `Cross-Origin-Resource-Policy: same-site`), and it stays true even through the proxy: **Google blocks Cloudflare datacenter IPs and answers the Worker with a 503 "Sorry..." page**, verified. So the client uses different sources than `generate.js` does. Before adding any feed, curl it through `?url=` and confirm real XML comes back — Bing News and Yahoo News search both dropped RSS and return HTML now.
  - 주요 뉴스: CNBC 마켓 + CNBC 주요 뉴스 RSS, deduped by link, sorted by `ts` (a real epoch, not the display date string — one feed's items would otherwise shuffle within a day), top 5. Two feeds because the markets feed alone posts too rarely to fill 5 slots without reaching a week back.
  - 종목 뉴스: `?news=<ticker>` per `WATCH` entry (Yahoo for US tickers, Naver for 6-digit KR codes; sometimes carries a `titleKo` the Worker translated). That feed is a *sector* feed, so each `WATCH` entry has a `re` that pulls company-matching items to the front — it does **not** filter the rest out, since empty per-stock blocks were the original complaint.
  - 앤트로픽: TechCrunch's `anthropic` tag feed, filtered by `/anthropic|claude/i` (the tag feed carries adjacent stories too).
- **제목 번역** (`translateNews`, 2026-08-07): the live feeds are English, and `koMapOf` can only reuse the morning `ko` for stories that already existed at 06:30 — so anything new stayed English. The fix is **Google Translate's public endpoint**, not an LLM: no key, no billing, `Access-Control-Allow-Origin: *` so the browser calls it directly. Use `translate_a/t?client=dict-chrome-ex`, which takes **repeated `q=` params and returns the translations as an array in the same order**; the more commonly cited `translate_a/single?client=gtx` translates only the *first* `q` and would need one request per headline. 8 titles come back in ~0.7s. Details worth keeping: results are cached in `localStorage` (`mytrans_v1`, 400 entries, oldest trimmed) so a re-open usually issues **zero** requests (verified: 2 requests on first load, 0 on reload); requests are chunked to keep the GET URL under ~1600 chars; `needsKo()` skips titles that are already Korean (Naver stock news, the Worker's own `titleKo`); a failure falls back to the `WORKER_URL` `?url=` proxy and then, silently, to the English original. `refreshAll` renders *before* awaiting the translation, so a slow translate never delays the news itself.
- **Schedule and mail** (`loadGoogleLive`): browser-side Calendar/Gmail APIs on one shared token, gated on `GOOGLE_WEB_CLIENT_ID` — see the 일정·메일 note under Known gaps. Off until that constant is filled in.

None of these paths calls an LLM, so opening or refreshing the app any number of times still costs zero tokens. Per-access traffic is now ~20 Worker requests (10 quotes + 9 news + …), 3 static JSON files (briefing, agenda, `push/state.json`), 0–2 Google Translate requests (0 once the day's headlines are cached), up to 10 Gmail requests if live mail is on, and (at most once per 30 min) 2 weather requests. **Growing that count is only acceptable because every one of those calls is free.** The single remaining per-day LLM call in the whole project is `generate-agenda.js`'s mail-importance pass, plus `generate.js`'s one `enrichKorean()` call, both in CI and both unaffected by how often the app is opened.

**Generation pipeline (implemented):** `.github/workflows/daily.yml` runs on a cron (`30 21 * * *` UTC = KST 06:30, plus manual `workflow_dispatch`, plus three retry firings) and calls `scripts/generate.js`, which writes `briefings/YYYY-MM-DD.json`, then regenerates `briefings/index.json` from the files actually present in the directory. It skips generation if the day's file already exists, and commits/pushes only when `briefings/` actually changed. It also skips when `dateKey` (the KST calendar day, not "now") falls on Saturday or Sunday — the US market is closed those days, so a Sat/Sun run would just regenerate the same already-published Friday session under a new filename. This must be checked against `dateKey`, not against the current moment converted to US Eastern time: a KST Monday-morning run is still Sunday night in US Eastern, so checking "now" would wrongly skip Monday's run too (which is the first run that's actually due to look for a new session). The weekday check (`weekdayOf()` in `generate.js`) computes this from `dateKey` using UTC-only `Date` methods (`` `${dateStr}T00:00:00Z` `` + `.getUTCDay()`) rather than `.getDay()` — `.getDay()` follows the *process's* local timezone, and on a GitHub Actions runner (UTC) the older `` `${dateKey}T00:00:00+09:00` `` + `.getDay()` form actually computed the wrong weekday (it only ever looked correct when tested on a KST-timezone dev machine). This was a live, unnoticed bug fixed 2026-08-05 — the code hadn't yet run through a real weekend since the Sat/Sun-skip feature was added.

**No *research* Claude in the market pipeline — but one cheap enrichment call (as of 2026-08-05):** `generate.js` makes exactly **one** LLM call per day, at the very end of `main()`: `enrichKorean()` sends the already-gathered headlines and change percentages to `claude-haiku-4-5` and gets back Korean one-line summaries (`ko`), a keep/drop flag per headline, and the card's `summary` line. Measured cost is well under a cent a day; it logs its own `usage` and dollar figure like `generate-agenda.js` does. Three rules keep it that way, and they are the whole point:
- **One call, not one per section.** Splitting it by news type re-bills the prompt each time.
- **No search or research tools, ever.** The thing that cost ~$1/day was the `web_search` server tool, *not* summarization — RSS replaced it, and re-adding it would undo the entire 2026-08-05 change.
- **Failure is non-fatal.** A missing `ANTHROPIC_API_KEY`, an API error, or unparseable JSON logs a warning and the briefing is written anyway, just without `ko`/`summary`. Never let this call gate the pipeline.

The paragraph below describes the *research* Claude usage that was removed and must not come back. Earlier versions used the Anthropic Messages API (`claude-sonnet-5` with the `web_search` server tool) first to verify index/stock prices, then (once the Worker took over prices, see below) to research and write Korean-language market news summaries. Both were removed on 2026-08-05: the trigger was cost — the companion "내 주식 현황" portfolio app (`ssangku2-tech/portfolio`) does all of its market data client-side against the same Worker and never calls an LLM, so its bill is near zero, while this app's daily Claude calls were running close to $1/day. Rather than just trim the `web_search` budget further, the user chose to drop AI-written Korean summaries/verification entirely and show raw source headlines instead. Concretely:
- **Prices** (`indices`/`stocks`): fetched directly from a Cloudflare Worker (`stock-proxy.ssangku2.workers.dev`, `?symbol=<ticker>` → `{price, prev, currency}`) — the same personal Worker the portfolio app uses to proxy Yahoo Finance/Naver quotes. Its source is not in this repo; treat its `?symbol=` contract as an external API. This part dates to 2026-08-04 (before the rest of the pipeline dropped Claude too) and works because the cron fires at 06:30 KST, hours after the US regular session closes, so Yahoo's `regularMarketPrice` is already frozen to that day's close. Per-symbol failure still yields `"확인 실패"`, with the existing four-times-a-day retry cron as the safety net — there is no separate fallback path.
- **News** (`news`/`aiNews`/`stocks[].news`): fetched from Google News RSS (`https://news.google.com/rss/search?q=...` — unauthenticated, no key) and parsed with a small hand-rolled regex parser in `generate.js` (`parseRssItems`) instead of an XML library, since this repo intentionally has no `package.json`/dependencies. Items are the *original* title/link/source/date — no Korean translation, no summarization, no "why did it move" commentary. `isFreshEnough()` keeps only items published after the previous briefing's `generatedAt` (its KST timestamp converted to a real instant) so the same story doesn't reappear day after day; comparing by calendar-day string alone was tried first and undercounted fresh items, because the cron's KST 06:30 run instant is still the previous calendar day in UTC (the same class of bug as the weekday check above, fixed together).
- **`sessionDate`/`sessionDateLabel`** (T, the most recently closed US session): computed by `computeSessionDate()` from `dateKey` alone (yesterday, or last Friday if `dateKey` is Monday) — no longer researched. This has **no US-holiday awareness** (a holiday Monday would mislabel T) — acceptable because the Worker's price is always the true latest close regardless of the label; only the display label could read one day off around a US market holiday.
- **`semiconductorNote`/`stockNotes`** (the old per-stock "왜 그렇게 움직였는가" commentary): removed entirely — producing that requires exactly the kind of research/reasoning this change stopped paying for. `stocks` items now only carry a `note` field in the Worker-failure case (`"Worker 조회 실패"`).
- **`summary`**: has had three meanings in one day, so check what produced a given file before trusting it. It began as a mechanical join of the index changePct values (`"S&P 500 +1.79%, 나스닥종합 +2.59%로 마감"`), was removed once the live 지수 스트립 made restating those numbers redundant, and now comes back from `enrichKorean()` as a Claude-written sentence about *what moved the market* — the prompt explicitly forbids listing numbers, precisely because the strip already shows them.

**Per-stock news (2026-08-05):** each `WATCH` entry has a `query` string and `main()` fires one Google News RSS search per stock in parallel, attaching up to 2 items as `stocks[].news`. The queries include the company name, not just the ticker, because bare tickers pull in homonyms (MU→university, AMD→macular degeneration). Freshness is preferred but **not** required here: a single stock's 24-hour window is empty often enough (SKHY especially) that `pickStockNews()` falls back to the single most recent item when nothing is fresh — so the same headline can persist for a few days under a quiet stock. That is deliberate; the alternative is a stock block with a price and nothing else. Palantir is no longer its own news section — PLTR is in `WATCH`, so its news arrives through this path and `aiNews` is Anthropic-only.

If `news`/`aiNews` quality or volume feels off, the first things to check are the RSS query strings in `generate.js` (the `fetchGoogleNewsRSS(...)` call sites in `main()`) and `isFreshEnough`'s cutoff — there is no `web_search`/`effort` budget to tune anymore. (Historical note: between 2026-08-01 and 2026-08-04, `generate.js` made two parallel Claude requests — `marketPrompt` and `aiNewsPrompt` — specifically to stop a shared `web_search` budget from letting semiconductor-price cross-verification starve the AI-news section. That whole mechanism, and the budget-tuning history that went with it, no longer applies now that neither request exists.)

**Agenda pipeline (implemented, 2026-08-02; rewritten 2026-08-03):** `.github/workflows/daily-agenda.yml` runs on its own cron (offset a few minutes from the market workflow's, independent workflow so overlap is harmless, four retry firings same as `generate.js`) and calls `scripts/generate-agenda.js`, which writes `agenda/YYYY-MM-DD.json`. Same skip-if-exists-for-the-day convention as `generate.js` — checked first, before any API calls, since the workflow retries up to four times a day and every retry would otherwise re-call both Google and Anthropic and re-commit. OAuth credentials (`GOOGLE_CLIENT_ID`/`GOOGLE_CLIENT_SECRET`/`GOOGLE_REFRESH_TOKEN`) are obtained once via `scripts/setup-google-auth.js` (a local one-time script, not run in CI) and stored as GitHub Actions secrets; the Google side is read-only by construction — the OAuth scopes granted (`calendar.readonly`, `gmail.metadata`) cannot write to the calendar or send mail no matter what the code does. `gmail.metadata` (not `gmail.readonly`) is deliberate: it covers everything the script reads (labels, From/Subject headers, snippet) and nothing more, so a leaked CI credential exposes headers rather than every mail body. **The rationale written here until 2026-08-10 was wrong in one respect** — it claimed `gmail.metadata` stays a "sensitive" rather than "restricted" Google scope, and that this is what avoids the formal security assessment. Google classifies `gmail.metadata` as **restricted**, same track as `gmail.readonly`. What actually makes this workable is the *personal use* exception to restricted-scope verification ("you are the only user of your app"), which applies regardless of which of the two scopes is used. So narrowing the scope is still right, just not for the reason recorded — and it never bought a different verification track.

**`gmail.metadata` forbids the `q=` search parameter, and until 2026-08-10 no code here respected that.** Any Gmail call carrying `q=` answers `403 "Metadata scope does not support 'q' parameter"` under this scope. Both `generate-agenda.js` and `index.html` used `q=is:unread`; both now filter with `labelIds=UNREAD&labelIds=INBOX` instead, and the unread total comes from `labels/INBOX`'s `messagesUnread` (an exact count, unlike the `resultSizeEstimate` it replaced). **Do not reintroduce `q=`** — the fix is not to widen the scope back to `gmail.readonly`. Why this hid for so long: the CI refresh token in use from 2026-08-01 to 2026-08-08 was **not** minted by `setup-google-auth.js` at all. The CI OAuth client is a **Web** client (not Desktop, as recorded here until 2026-08-10 — its only registered redirect was `https://developers.google.com/oauthplayground`), and that token was issued through OAuth Playground with `gmail.readonly`, which does accept `q=`. The scope written in this repo only actually took effect on 2026-08-10, when the token was re-minted through the script, and the 403 surfaced immediately. The Desktop-client assumption cost real time during that re-mint; the client now also has `http://127.0.0.1:53682/oauth2callback` registered, so the script path works on it.

**The consent screen's "Testing" status caps refresh tokens at 7 days, and on 2026-08-08 that finally broke the cron for real.** The refresh token was issued 2026-08-01 (`f625b14`); the last successful run was 2026-08-07 23:27 UTC and every firing from 2026-08-08 21:27 UTC on died with `invalid_grant` / `Token has been expired or revoked`. Nothing in the code or the secrets was wrong — the token simply aged out. Two things worth knowing if it happens again: only this workflow is affected (`daily.yml`/`alerts.yml` never touch Google, and the phone's live 일정 keeps working because the GIS token flow issues no refresh token at all — the live 메일 half was in fact broken across this whole window, but for the unrelated `q=` reason above, not the token — what is lost is the `agenda/` file, i.e. the offline fallback, the morning push body, and the Claude-written mail summaries); and **the fix has an order**. Publish the OAuth consent screen to production *first* (Google Cloud Console → API 및 서비스 → OAuth 동의 화면 → 앱 게시), *then* re-run `setup-google-auth.js` and update the `GOOGLE_REFRESH_TOKEN` secret — a token minted while the screen is still in Testing carries the 7-day clock with it, so re-issuing without publishing just buys another week. Publishing unverified means an "확인되지 않은 앱" interstitial during consent (고급 → 이동) and a 100-account cap, neither of which matters for a single personal account.

Calendar: fetches today+tomorrow (KST, converted to UTC for the Calendar API) from the primary calendar; `allDay` = response has `date` but no `dateTime`. Getting the KST day-boundary math wrong is this script's equivalent of the market pipeline's "거래일 혼동" bug — there's no other verification step for calendar data, since it's the user's own already-settled data with nothing to cross-source-verify. Mail: unlike the calendar half, **this half does call the Claude API** (`claude-haiku-4-5`, cheap/fast — deliberately not `claude-sonnet-5`) — it sends only the subject/sender/snippet (never the full body) of up to 15 unread messages and asks Claude to pick up to 5 as "important" with a one-line Korean summary each, filtering out ads/newsletters/automated notices. `unreadCount` is a separate `labels/INBOX` query against Gmail (a real total, not capped by the 15-message detail slice used for the importance pass — and exact, since it reads the label's own `messagesUnread`). Because this is the only LLM call left in the project, `summarizeMails()` logs the response's `usage` and a computed dollar figure (`[claude] claude-haiku-4-5 입력 N · 출력 M 토큰 ≈ $X`) — the Actions log is therefore the place to read *measured* cost rather than estimating it. Ballpark: ~3k input + ~600 output tokens, about $0.006/day (~$0.2/month) at Haiku 4.5's $1/$5 per MTok. A sudden 4× jump means the skip-if-exists guard stopped working and all four retry firings are calling the API. Self-audit is lighter than the market pipeline's — no numeric cross-check needed — but the skip-if-exists check and the separate true-total unread query are the two things that keep this cheap and correct under the four-retries-a-day schedule.

## Commands

There is no build/lint/test tooling for the client. To work on it locally, serve the directory with any static file server (e.g. `npx serve .`) — use a server rather than opening `index.html` via `file://`, since service workers and `fetch` of `briefings/*.json` require an http origin.

To manually generate a day's briefing (normally done by the cron workflow):
```
node scripts/generate.js [YYYY-MM-DD]
```
No API key needed — as of 2026-08-05 this script only talks to the Worker and Google News RSS (see the Generation pipeline note above). Defaults to today in KST if no date given; skips if `briefings/YYYY-MM-DD.json` already exists.

To manually generate a day's agenda (normally done by the cron workflow):
```
GOOGLE_CLIENT_ID=xxx GOOGLE_CLIENT_SECRET=xxx GOOGLE_REFRESH_TOKEN=xxx node scripts/generate-agenda.js [YYYY-MM-DD]
```
`worker/push-store.js` is not built or deployed by anything in this repo — it is pasted into the Cloudflare dashboard by hand (see the push-subscription store note under Known gaps). It needs a KV binding named `PUSH_KV` and two environment variables, `WRITE_TOKEN` (must match `PUSH_STORE_TOKEN` in `index.html`) and `ADMIN_TOKEN` (must match the `PUSH_STORE_ADMIN_TOKEN` GitHub secret). The repo variable `PUSH_STORE_URL` points CI at it.

To set up the morning push notification (one-time, local — not run in CI):
```
node scripts/setup-push.js
```
It writes the key pair to `push-keys.txt` (gitignored) and prints **only** the file path — the private key deliberately never reaches stdout, because a terminal capture is exactly how it leaks (running this as `! node scripts/setup-push.js` in Claude Code would put stdout straight into the conversation; that happened once, and the key had to be regenerated). Open the file yourself: paste the **public** key into `index.html`'s `VAPID_PUBLIC_KEY` and commit it, put the **private** key in the `VAPID_PRIVATE_KEY` secret, then press 알림 받기 in the app and put the subscription JSON in `PUSH_SUBSCRIPTION`. Delete `push-keys.txt` afterwards. Only two secrets are needed — `send-push.js` reads the public key out of `index.html` rather than taking a third. Details in the Push notifications note below.

Same defaulting/skip behavior as `generate.js`, against `agenda/YYYY-MM-DD.json`. To obtain the three `GOOGLE_*` values in the first place, run `node scripts/setup-google-auth.js` locally once (opens a browser OAuth consent flow, prints the values to your own terminal — never paste them into chat, commits, or logs) after creating a Google Cloud OAuth client; see the Agenda pipeline note above for the scope rationale.

## Architecture

**`index.html`** is the entire application: inline `<style>` for all CSS, inline `<script>` for all logic. There is no router or component framework. The structure is:
- A single `state` object: `{ briefing, shownDate, agenda, agendaDate, live, quoteStatus, liveNews, newsStatus, liveMail, liveEvents, googleStatus, googleError, googleGrants, refreshedAt, refreshing, assetForm, assetError, assetsRevealed, pinPrompt, pinInput, pinError, alertCopied, pushSub, pushError, pushCopied, pushServer, pushHash, pushChecked, pushStale, pushDead, pushConfirm }`.
- One renderer function per card — `renderUSBriefing(b)` (관심 종목/주요 뉴스/앤트로픽), `renderSchedule(a)`, `renderMail(a)`, `renderLearning()`, `renderAssets()`. `renderUSBriefing`/`renderSchedule`/`renderMail` take the *daily file* as their argument and read the live data off `state` — the argument is the fallback, not the source. Each computes its own body HTML (handling its own loading/empty state) and hands it to `cardShell(id, icon, title, sub, bodyHtml, defaultOpen)`, which wraps it in the accordion header (clickable, chevron) and the collapsible `.card-body`. Open/closed state per card id is remembered in `localStorage` (`mycard_open_v1`) via `isCardOpen`/`toggleCard`, overriding `defaultOpen` once the user has toggled a card at least once. `'us'` is the only card with `defaultOpen: true`.
- `renderIndexStrip()` is deliberately *not* a card: the three indices render into `#idx-strip` right under the header so they're readable without expanding anything. Because of it, `renderUSBriefing` no longer emits a 주요 지수 block — don't add one back, it would be the same three numbers twice.
- A single `render()` that fills the header, the 갱신 status line, the index strip, the refresh button's label/subtext, then joins the section strings into `#sections`. It is called after every state change (including `toggleCard`), so it must stay cheap and side-effect-free.

**Data flow:** `refreshAll(force)` is the single entry point — startup, the refresh button, and the `visibilitychange` auto-refresh all call it, so no path can be "more up to date" than another. It sets `state.refreshing`, renders (button reads 갱신 중…), then runs `loadBriefing()` + `loadAgenda()` + `loadQuotes()` + `loadNews()` + `loadGoogleLive(false)` + `loadWeather(force)` in parallel and renders once more. `visibilitychange` re-runs it only when the app has been backgrounded for more than 3 minutes (`AUTO_REFRESH_AFTER`) — an installed PWA can stay alive for days, so without this the user sees yesterday's screen. The refresh button therefore is not the way to get fresh data; it exists for "지금 당장 다시" and for forcing past the weather cache, and its subtext states the last refresh time so the user can tell whether pressing it would change anything.

`loadBriefing()` fetches `briefings/${todayKST()}.json` with `cache:'no-store'`. A 404 is a normal outcome, not an error — it means today's briefing doesn't exist yet. In that case it reads `briefings/index.json` (a sorted array of available date keys) and falls back to the most recent one, recording it in `state.shownDate` so the card can say which day it's showing.

`loadAgenda()` does the same against `agenda/` (`agenda/index.json`, written by `generate-agenda.js` on every run), recording `state.agendaDate`. The fallback is more useful here than it looks: `generate-agenda.js` always fetches **today + tomorrow**, so yesterday's file still contains today's events — `renderSchedule` drops events whose date is before today and shows the rest. Mail has no such property (unread count and the importance pass are that morning's snapshot), so both cards render a `.stale` notice when `isAgendaStale()`. Before 2026-08-05 there was no fallback at all: a missing file rendered the empty state even when yesterday's file held today's schedule.

**Live quotes (`loadQuotes`):** client-side mirrors of `generate.js`'s `INDICES`/`WATCH` drive 10 parallel Worker requests. **The `name`/`ticker` values must stay identical to `generate.js`'s** — `quoteFor()` matches live rows against briefing rows by those keys to swap in the fresh price, and a rename on one side silently reverts that symbol to the 06:30 close. (`WATCH`'s `re` field is client-only — `generate.js` has no equivalent and doesn't need one.) Per-symbol failure is silent and falls back to the briefing file's value; `state.quoteStatus` goes `'fail'` only when *every* symbol failed, and a previous successful `state.live` is kept rather than cleared. `up` is a tri-state here: `true`/`false`/neither (`'확인 실패'` rows from the briefing file), so every renderer picks a class from `up === true ? 'up' : up === false ? 'down' : 'flat'` — never a bare `up ? … : …`, which would paint an unknown value blue for 하락.

**Briefing file shape** (`briefings/YYYY-MM-DD.json`, produced by `scripts/generate.js`), current (2026-08-05+) form:
```json
{
  "date": "2026-08-05",
  "sessionDate": "2026-08-04",
  "sessionDateLabel": "2026년 8월 4일(화) 미국 동부시간 정규장 마감",
  "generatedAt": "2026-08-05 06:30",
  "indices": [{"name":"S&P 500","price":"7,736.52","changePct":"+1.79%","up":true}],
  "news": [{"title":"Dow surges 900 points, S&P 500 closes above 7,700...","link":"https://news.google.com/rss/articles/...","date":"2026-08-04","source":"CNBC"}],
  "stocks": [{"name":"엔비디아","ticker":"NVDA","price":"$211.94","changePct":"+2.56%","up":true,
              "news":[{"title":"Nvidia Tops Key Level As AMD Heads Into Earnings","link":"https://news.google.com/rss/articles/...","date":"2026-08-04","source":"Investor's Business Daily"}]}],
  "aiNews": [{"company":"Anthropic","title":"Anthropic Inks $10 Billion Computing Deal...","link":"https://news.google.com/rss/articles/...","date":"2026-08-04","source":"Bloomberg.com"}]
}
```
`date` is the KST calendar day the file is named for; `sessionDate`/`sessionDateLabel` are the actual US trading day (T) the numbers came from — these can differ (e.g. a Monday-morning KST file reporting Friday's session), and as of 2026-08-05 this is computed by date arithmetic, not researched (see the Generation pipeline note above — no US-holiday awareness). `price`/`changePct` are pre-formatted display strings (the client does no number formatting); `up` is an explicit boolean rather than derived from a signed number. `news`/`stocks`/`aiNews` (and `stocks[].news`) are each optional — the renderers only emit a block for ones that are non-empty, so older or partial briefing files degrade gracefully.

**Almost nothing in this file is what the user normally sees** — as of 2026-08-07 the client overwrites `indices`/`stocks` prices *and* `news`/`aiNews`/`stocks[].news` with values fetched at access time. The file still matters: it is what renders when those fetches fail or the user is offline, and it remains the only source of the Claude-written text (`summary`, `ko`). `sessionDateLabel` is no longer used as the card sub when live news is on (it would date the card to last night's close while the headlines are from minutes ago); it survives as the caption under `summary`, which really is about that session.

Each `news`/`aiNews`/`stocks[].news` item is `{title, link, date, source}` plus an optional **`ko`** — a Korean one-line summary written by `enrichKorean()` — and, at runtime only, **`koT`**, the machine-translated title `translateNews()` attaches in the browser (never written to the JSON files). `title` always stays the untouched original headline (it is what the link goes to, and what both the translation cache and `koMapOf` key on); the Korean forms are what the UI shows. Precedence as of 2026-08-07: 관심 종목 blocks show `ko || koT || title` (one compact line), and 주요 뉴스/앤트로픽 now put `koT || title` in the link with `ko` underneath as the body — i.e. the headline itself reads Korean, and the original survives as the link's `title` attribute rather than as a visible second line (stacking both would double the card's length). Files generated without an API key simply have no `ko` and render in English — that is a supported state, not a bug. Files from before 2026-08-05 instead have `{title, body, date}` (no `link`/`source`) — a Claude-written Korean summary with no source link. `index.html`'s renderers branch on `n.link` being present to support both shapes in the files already committed under `briefings/`; don't remove the `body` branch without migrating or deleting those older files. Pre-2026-08-05 files also carry `semiconductorNote` (no longer rendered) and Palantir entries inside `aiNews` (filtered out by `company`, since Palantir moved to `stocks[].news`). A price/changePct value of `"확인 실패"` means the Cloudflare Worker query for that symbol was unreachable or 404'd at generation time — expected occasionally, not a bug.

**`state`** carries `{ briefing, shownDate, agenda, agendaDate, live, quoteStatus, liveNews, newsStatus, liveMail, liveEvents, googleStatus, googleError, googleGrants, refreshedAt, refreshing, assetForm, assetError, assetsRevealed, pinPrompt, pinInput, pinError, alertCopied, pushSub, pushError, pushCopied, pushServer, pushHash, pushChecked, pushStale, pushDead, pushConfirm }`. Renderers read this global directly rather than taking it as an argument (only `renderUSBriefing(b)`/`renderSchedule(a)`/`renderMail(a)` take their data), so a new card's empty state should follow the same shape.

**Time handling:** all date/time logic is KST, computed by offsetting UTC by +9h (`todayKST`, `hourKST`) rather than using `toLocaleString` with a timezone. `greetingKST()` maps the KST hour to one of five greetings. Keep new time logic on the same +9h-offset approach so the whole app agrees on what "today" is. `sessionDate`/`sessionDateLabel`, by contrast, describe the US trading day and are produced server-side by `generate.js` — the client never computes them.

**Service worker (`sw.js`)** uses a versioned cache name (`daily-brief-vN` — bump on asset changes to bust old caches; currently v40). Fetch handling, in order: non-GET and external API/font hosts bypass the cache entirely (`workers.dev` is in that list and must stay there — caching that Worker would serve a stale price *and* stale news and defeat the whole access-time refresh; `accounts.google.com`/`googleapis.com` were added with live schedule/mail); `/briefings/*`, `/agenda/*`, `/alerts/*` and `/push/*` are network-first with a cache fallback (so an offline user still sees the last data they received — and a cached `push/state.json` would either keep showing a warning that's already fixed or hide a new one); **the app shell is network-first too**; everything else (icons, manifest) is cache-first with a background network fill. The app shell (`./`, `index.html`, `manifest.json`) is precached with `addAll` at install; the icons are precached separately with `allSettled` because `addAll` fails the entire install if any one URL 404s.

Two things about the shell branch are load-bearing and easy to undo by accident:
- **It fetches with `cache:'no-store'`, and by URL string rather than passing `e.request`.** GitHub Pages serves `index.html` with `Cache-Control: max-age=600`, so a plain `fetch(e.request)` still lets the browser's HTTP cache return HTML up to 10 minutes old — network-first in the service worker alone does *not* defeat that. The URL-string form is required because a `Request` whose `mode` is `navigate` cannot be passed to `new Request(...)`.
- **It stores the response under the `'./index.html'` cache key**, not under the request URL, so the offline fallback resolves whether the user arrived at `/daily-brief/` or `/daily-brief/index.html`.

**`sw.js` was dead code until 2026-08-05** — `index.html` never called `navigator.serviceWorker.register()`, so nothing was ever precached, no fetch handler ever ran, and every `daily-brief-vN` bump before v20 was ceremonial. The registration was added at the bottom of `index.html`'s script only *after* the shell became network-first; registering it while the shell was still cache-first would have made every deploy land one launch late. If you are debugging "my change isn't showing up", check in this order: (1) is the Pages deploy finished, (2) is the ~10-minute HTTP cache still holding (hard-reload to confirm), (3) service worker state.

## Conventions specific to this codebase

- 단일 `index.html` 구조를 유지할 것 — HTML/CSS/JS를 별도 파일이나 빌드 단계로 분리하지 않는다.
- `index.html` 또는 `sw.js`를 수정할 때는 `sw.js`의 `CACHE` 버전 문자열(`daily-brief-vN`)을 반드시 올린다 — 그렇지 않으면 기존 캐시가 새 자산을 가리지 못한다.
- 커밋 메시지는 한국어로 작성한다.
- 대화는 한국어로 진행한다.
- UI 문구와 코드 주석은 한국어로 작성한다.
- `esc()` must wrap any data-supplied string interpolated into an HTML template string — this app has no framework-level auto-escaping, and briefing content will come from network JSON.
- All colors go through the CSS custom properties in `:root`; don't hardcode hex values in rules. Note the Korean market convention baked into the tokens: `--up` is red, `--down` is blue.
- 새 카드를 추가할 때는 `renderXxx()` 함수 하나를 만들어 본문 HTML을 계산한 뒤 `cardShell(id, icon, title, sub, bodyHtml, defaultOpen)`에 넘기고, 그 결과를 `render()`의 `sections` 배열에 push 하는 패턴을 따른다 — 카드마다 자기 빈 상태는 직접 처리하되, 헤더·접기/펼치기(아코디언)는 `cardShell`에 위임한다. `id`는 다른 카드와 겹치지 않는 짧은 문자열(예: `'us'`, `'mail'`)이어야 한다.
- 날씨 위젯은 Open-Meteo(현재 기온 + 오늘 최고/최저) + BigDataCloud의 클라이언트용 역지오코딩(`reverse-geocode-client`, API 키 불필요)을 함께 쓴다. 결과는 `localStorage`(`myweather_v1`)에 30분간 캐시하고, 캐시가 살아 있으면 위치 권한 조회(`getPosition`)조차 하지 않는다 — 앱을 열 때마다 지오로케이션을 타면 첫 화면이 눈에 띄게 늦다. 새 외부 API를 붙일 때는 `sw.js`의 캐시 우회 목록(`workers.dev`/`open-meteo.com`/`bigdatacloud.net`/`accounts.google.com`/`googleapis.com`/...)에도 호스트를 추가해야 한다 — 안 그러면 서비스워커가 그 응답을 캐시-우선으로 가로챈다.
- 접속마다 도는 호출은 **과금되지 않는 것만** 늘린다. 현재 1회 접속 = Worker 시세 10건 + Worker 뉴스 9건 + 정적 JSON 3건(브리핑·일정·`push/state.json`) + 구글 번역 0~2건(그날 제목이 캐시되면 0건) + (실시간 일정·메일을 켰다면) Calendar 1건 + Gmail 10건 + (30분에 한 번) 날씨 2건이고, LLM 호출은 0건이다. 2026-08-07에 붙인 제목 번역이 이 규칙의 전형적인 예다 — 번역은 Claude 를 부를 일처럼 보이지만 키도 과금도 없는 공개 엔드포인트로 되므로 접속 경로에 넣어도 된다. 2026-08-07에 뉴스·일정·메일을 접속 시점으로 옮기며 호출 수가 두 배가 됐는데, 전부 무료 경로라 문제가 없다는 게 판단 근거였다 — 호출 건수 자체가 규칙이 아니다. 반대로 클라이언트에 Claude API를 붙이는 것은 한 건이어도 안 된다. 앱을 열 때마다 토큰이 나가므로(`generate.js`에서 Claude를 걷어낸 이유와 같은 문제가 접속 횟수만큼 커진다) 넣지 않는다. 그래서 한국어 요약(`ko`)·`summary`·메일 중요도 요약은 계속 매일 CI에서만 만든다.
- `generate.js`는 (2026-08-05부터) Claude API를 전혀 호출하지 않는다 — `marketPrompt`/`aiNewsPrompt` 같은 프롬프트 구조는 더 이상 없다. 지수·종목의 price/changePct는 `fetchLiveIndicesAndStocks()`가 Cloudflare Worker에서, 뉴스(`news`/`aiNews`/`stocks[].news`)는 `fetchGoogleNewsRSS()`가 Google News RSS에서 받아온다. 이 두 데이터 소스에 다시 Claude 검증·요약을 끼워 넣지 말 것 — 비용 때문에 일부러 뺀 것이다(위 "No Claude in the market pipeline" 참고). Worker 호출을 건드릴 때는 `WORKER_URL`이 이 레포 소유가 아닌 외부 계약(포트폴리오 앱의 개인 Cloudflare Worker)이라는 점을, RSS 호출을 건드릴 때는 이 레포에 `package.json`이 없어 XML 파서를 정규식(`parseRssItems`)으로 직접 짰다는 점을 유의한다.
- Mobile-first: the layout is capped at `max-width:520px` and uses `env(safe-area-inset-*)` padding. Test at phone widths, and keep tap targets full-width like `.refresh`.

## 다음에 할 일 (2026-08-05 기준)

며칠 실제로 써봐야 판단이 되는 것들이라 일부러 미뤄둔 항목이다. 하루치 데이터로 더 손보면
그날 시장에만 맞춘 설정이 된다.

**설정만 하면 되는 것 (코드 수정 없음)**
- **실시간 일정·메일 — 2026-08-07에 켰다.** `GOOGLE_WEB_CLIENT_ID` 에 웹 애플리케이션 유형 OAuth 클라이언트 ID(`576621681088-…`, 승인된 JavaScript 원본 `https://ssangku2-tech.github.io`)를 넣어 커밋했다. CI 가 쓰는 데스크톱 클라이언트와는 별개의 클라이언트이고, 둘 다 같은 프로젝트·같은 동의 화면(테스트 상태)을 쓴다. 2026-08-07에 폰에서 실제로 연결해 동작을 확인했다 — 헤드리스로는 구글 세션이 없어 왕복을 못 태우므로, 이게 이 경로가 실제로 돈다는 유일한 확인이다. **며칠 써 보고 판단할 것**: 토큰이 1시간짜리라 그보다 오래 지나 앱을 열면 카드에 `실시간으로 다시 불러오기` 버튼이 뜬다. 이 한 번 누르는 게 성가시면, 그때는 일정·메일을 아침 파일로 되돌리는 게 아니라 "마지막으로 받아둔 값 + 기준 시각"을 그대로 두는 지금 방식이 맞는지부터 다시 본다(자동 재발급은 불가능하다 — Known gaps 의 GIS 항목 참고).
- `ALERT_SYMBOLS` 시크릿 → **2026-08-06에 17종목 등록됨**(`WATCH` 7종목과 합쳐 24종목 감시). 보유 22종목 중 **레버리지·인버스 5종목은 일부러 뺐다** — 아래 참고. 종목을 사고팔면 앱의 내 자산 카드 → `급변동 알림 종목 내보내기` 로 다시 내보내 갱신해야 하는데, **그 버튼은 보유 종목을 전부 내보내므로 레버리지·인버스를 다시 손으로 걸러내야 한다**(빠진 5종목: `252670.KS` 곱버스2X, `114800.KS` 인버스, `122630.KS` KODEX 레버리지, `0193W0.KS` 삼전 레버리지, `0193T0.KS` 하이닉스 레버리지). **이 갱신은 대신 해줄 수 없는 종류의 일이다** — 보유 종목은 폰 브라우저의 `localStorage` 에만 있고, 개발 PC 의 Chrome 에는 `portfolio_v2` 자체가 없다(2026-08-06 확인). 서버도, 여기서 도는 도구도 그 값을 읽을 방법이 없으니 사용자가 붙여넣어 주는 수밖에 없다.
  - 등록 직후 29종목을 Worker 에 직접 조회해 전부 시세가 잡히는 걸 확인했다 — `0193W0.KS`(삼전 레버리지)·`0193T0.KS`·`0008S0.KS` 같은 비표준 코드와 `.KQ`(코스닥), `SPCX` 까지 전부 응답한다. 새 종목을 넣었는데 알림이 안 온다면 임계값 문제일 가능성이 높지, 심볼이 죽어서는 아니다(그래도 한 번 조회해 보는 게 빠르다).
  - **레버리지·인버스를 뺀 이유:** 22종목 전부 넣었을 때 8% 를 넘은 6종목 중 5종목이 2배 상품이었다(`하이닉스 레버리지` −20.6%, `삼전 레버리지` −12.8%, `KODEX 레버리지` −10.3%, `곱버스2X` +10.3%). 2배 상품은 원지수가 4% 만 움직여도 임계값을 넘으니 알림을 독점한다. 대응은 임계값을 더 올리는 게 아니라 종목을 빼는 것이다 — 임계값을 올리면 정작 일반 종목의 진짜 급변동(같은 날 `삼성전기` −9.4%)을 놓친다. 빼고 나니 24종목 중 8% 초과가 2건으로 줄었다.
  - 남아 있는 것 중 `SPCX`(스페이스X, 그날 −13.6%)는 레버리지가 아니라 개별 종목이라 일부러 남겼다. 이것만 계속 시끄러우면 다음 후보다.
- `ALERT_THRESHOLD` (Variables) → **2026-08-06에 8로 등록했다**(그전엔 변수 자체가 없어 기본값 5로 돌고 있었다). 5일 때 7종목 중 5종목이 걸려 알림이 무의미해졌던 게 이유다. 8로 며칠 받아보고 다시 판단.
- `VAPID_PUBLIC_KEY` 시크릿은 이제 안 쓰인다(`send-push.js`가 `index.html`에서 읽는다). 지워도 되고 둬도 무해하다.

**며칠 지켜보고 판단할 것**
- 종목 뉴스가 특정 종목에서 계속 비는지. 2026-08-05에는 7종목 중 3종목이 시세만 나왔다(후보 5건이 전부 투자 권유형이라 전멸). 계속 그러면 `pickStockNews`의 후보 수를 5→8로 올리거나 낚시성 판정을 완화한다. 단, 후보를 30→41건으로 늘렸을 때 오히려 선별 결과가 줄어든 전례가 있다 — 병목은 후보 수가 아니라 필터 기준이었다.
- 아침 알림이 매일 제때 오는지. Actions 크론은 수십 분 밀린다.
- 급변동 알림이 실제로 얼마나 자주 울리는지.

**아직 안 만든 것**
- 일정 시작 전 리마인더. Actions 크론이 수십 분 밀려 "30분 전 알림"이 성립하지 않는다. 하려면 Cloudflare Worker의 Cron Trigger로 옮겨야 한다(정확하지만 구조가 하나 늘어난다).
- 자산 기준 알림(보유 평가액이 N% 변할 때). 보유 수량이 기기에만 있어 서버가 계산할 수 없다. 지금의 종목 단위 급변동 알림이 현실적인 대체재다.
- 워크플로 실패 알림. 브리핑 생성이 실패하면 GitHub 기본 메일로만 알 수 있는데, 그 메일이 앱의 메일 카드에 뜨긴 한다. 푸시로도 받고 싶으면 `daily.yml`에 `if: failure()` 스텝을 하나 붙이면 된다.

## Known gaps

**Push notifications (2026-08-05):** one notification a morning, fired by `daily.yml` right after the briefing is committed. Three pieces:
- `scripts/setup-push.js` — local one-time VAPID key generator. It writes to the gitignored `push-keys.txt` and prints only the path; **do not "improve" it by printing the private key**, even for convenience. The first version did print it, and running it as `! node scripts/setup-push.js` put the key straight into the conversation transcript — file output is what makes the documented invocation safe. Only two secrets exist: `VAPID_PRIVATE_KEY` and `PUSH_SUBSCRIPTION`. The public key lives in `index.html` (committed, safe) and `send-push.js` reads it from there, so it can't drift out of sync with what the browser subscribed against. A subscription endpoint is a capability URL — anyone holding it plus the private key can push to the phone — so it must never be committed.
- `scripts/send-push.js` — sends a **payload-free** push. This is the design decision worth preserving: carrying a payload would require ECDH + HKDF + AES-GCM, which means either hand-rolled crypto or the `web-push` package, and this repo has no `package.json`. A bare push needs only a VAPID JWT, which is ~20 lines of `node:crypto`. Two easy-to-miss details: the JWS signature must be P1363 (`dsaEncoding: 'ieee-p1363'`) because Node emits DER by default and push services reject it, and `exp` must be under 24h.
- `sw.js`'s `push` handler — since the push is empty, the handler fetches today's `briefings/` and `agenda/` JSON and builds the text itself. This is why the notification reflects the data at *delivery* time, not send time. `userVisibleOnly: true` obliges it to call `showNotification` on every push, so the body-building is wrapped in try/catch with a fixed fallback string — never add an early return that could skip the call.

**Stock alerts (2026-08-05):** `.github/workflows/alerts.yml` runs `scripts/check-alerts.js` every 30 minutes during both market sessions (KST 09:00–15:30 for KRX, 22:30–05:00 for the US; the US window is padded because DST shifts it an hour) and pushes when a watched symbol moves at least `ALERT_THRESHOLD` percent (default 5) against its previous close. **This is a "something moved, go look" nudge, not a trading signal** — GitHub Actions cron routinely runs tens of minutes late, and nothing here tries to hide that.

- **Which symbols:** the hardcoded `WATCH` list ∪ the optional `ALERT_SYMBOLS` secret. The server cannot read the phone's `localStorage`, so holdings reach it only via the app's 급변동 알림 종목 내보내기 button, which copies `SYMBOL=이름` pairs. It exports **symbols and display names only** — quantities and average prices are deliberately excluded, since the threshold check doesn't need them and including them would put the portfolio's size in a secret. It also exports **every account**, not just the 종합 tab scope the 자산 card displays (see the 자산 card note below for why the two scopes differ). The list goes stale whenever holdings change and has to be re-exported; `WATCH` is always included so a stale secret still leaves the core names covered. `check-alerts.js` keeps its own copy of `WATCH` — keep it in sync with `generate.js`.
- **Dedupe:** `alerts/YYYY-MM-DD.json` records what already fired today, so a stock sitting above the threshold all session alerts once rather than on every cron tick. Nothing is committed and no push is sent when nothing new crosses.
- **How the notification gets its text:** the same payload-free trick as the morning push. `check-alerts.js` writes `alerts/latest.json`, the workflow commits it, **waits 90 seconds for the Pages deploy**, and only then sends the push; `sw.js` fetches that file and shows the names and percentages. The wait is load-bearing — push without it and the service worker reads the previous alert. The handler decides which kind of notification to show by the file's `at` timestamp: fresher than 20 minutes means a stock alert (tag `stock-alert`), otherwise the morning briefing (tag `daily-brief`). 20 minutes is under the 30-minute cron interval, so a stale alert file can't hijack the next morning's briefing.

The briefing send step is gated on `steps.gen.outputs.generated == 'true'`, which `generate.js` writes only when it actually creates a file; the four retry firings therefore produce at most one notification a day. `send-push.js` exits 0 for everything except one case: a **404/410 exits 1 and fails the workflow on purpose**. Those two statuses mean the subscription is permanently gone (notifications turned off, service worker replaced, browser cleanup) and no amount of retrying fixes it — someone has to re-subscribe in the app and update `PUSH_SUBSCRIPTION`. Failing makes GitHub send its "Run failed" mail, which then shows up in the app's own 메일 card; a 410 once passed silently under a green checkmark and went unnoticed for hours. Everything else stays exit 0: missing secrets (notifications are optional and must never break the briefing), and 5xx/429/network errors (transient — the next cron retries, and the alerts workflow runs dozens of times a day, so failing on those would bury the one failure that matters). Both send steps run *after* their data is committed, so a failure here never costs the briefing or the alert record.

**Recovering from a 410 needs a button, because the phone doesn't know its subscription died (2026-08-10).** A 410 is what the *push service* tells the server; the browser can keep handing back the same dead subscription object from `getSubscription()`, so the app renders 알림 켜짐 and the 구독 정보 it offers to copy is the dead endpoint — updating the secret with it changes nothing. This actually happened: pushes stopped 2026-08-08 (KST) and every send since 410'd, while the app looked fine. `resubscribePush()` is the recovery path — `unsubscribe()` then `subscribe()` again — and it's a link in the collapsed status row, not hidden behind 구독 정보, since it's the only way out of a state where the visible UI says everything is fine. Two details: permission is requested **before** `unsubscribe()` (awaiting the unsubscribe first breaks the user-gesture chain, so a permission prompt could be ignored), and `pushBusy` is cleared in a `finally` — the permission-denied branch `return`s from inside the `try`, and resetting after the try/catch left the button stuck on 다시 구독 중… forever. Re-subscribing always mints a new endpoint, so `state.pushRenewed` makes the panel auto-expand and say the secret *must* be updated, replacing the usual "이미 등록했다면 다시 넣지 않아도 됩니다".

**Replacing the app icon means reinstalling the PWA, and reinstalling kills the push subscription (2026-08-11).** A second 410 outage, three days after the one above, with a different cause: the subscription was re-minted and the secret updated at 12:12 KST and a manual test send returned **201** — then `icon-192.png`/`icon-512.png` were replaced at 12:34 and the next morning's send 410'd. Android bakes a PWA's icons into the WebAPK at install time, so seeing a new icon requires uninstall + reinstall, and the reinstall creates a fresh service worker registration that the old subscription does not survive. `PUSH_SUBSCRIPTION`'s own last-updated timestamp is what proves this class of failure: if it predates whatever touched the install, the secret is stale no matter what the phone shows. Web push subscriptions have no expiry (`expirationTime` is null on FCM) and a VAPID key mismatch answers 403, not 410 — so a 410 always means the subscription is gone from the *browser* side, and the list of ways that happens is short: `unsubscribe()`, service worker registration removed, site data cleared, PWA reinstalled, notification permission revoked. **Anything that changes the manifest or icons belongs on that list**, so re-subscribe and update the secret as part of shipping such a change.

**The device-side `localStorage` baseline (`mypush_synced_v1`, 2026-08-12) was the wrong shape and is gone (2026-08-13).** It recorded the endpoint at the moment 구독 정보 복사 was tapped, as a stand-in for "this is what's in the secret". It missed the two cases that actually happen: if the subscription rotates *before* any copy there is no baseline at all (that was the 2026-08-12 outage — the working subscription was copied at 10:20 KST, the detection code shipped at 10:26, so no baseline was ever written and the app stayed calm while the subscription died the same afternoon), and a reinstall wipes site data and takes the baseline with it. It also cleared the warning on *copy*, not on the secret actually being updated — exactly the step people forget.

**The comparison is now against what CI really has: `push/state.json`.** `scripts/push-state.js` writes `{endpointHash, status, at, sentAt}`, where `endpointHash` is `sha256(endpoint of PUSH_SUBSCRIPTION).slice(0,16)` — a fingerprint, never the endpoint, because this file is committed to a public repo and served over Pages, and an endpoint plus the VAPID private key is the capability to push to the phone. `status` is the last send result (`0` = never attempted with this subscription — which is what triggers the verification send described below; `-1` = attempted but the request threw before any response). The app fetches it in `refreshAll` (`loadPushState`), hashes its own endpoint with `crypto.subtle`, and `updatePushStale()` produces exactly one of three states:
- fingerprints differ → **`pushStale`** (⚠️ 시크릿과 어긋남). The fix is pasting the secret, so the panel auto-expands with the JSON, and **다시 구독 is hidden entirely** — tapping it here mints yet another endpoint and the value to paste just changes again.
- fingerprints match but `status` is 410/404 → **`pushDead`** (⚠️ 구독이 폐기됨). The browser still hands back a live-looking subscription object, so only the server can tell us this. 다시 구독 is the fix and is the one thing surfaced.
- otherwise → calm 알림 켜짐.

Two guards are load-bearing. **No server record means no judgement**: `updatePushStale` returns early when `push/state.json` is missing/empty, and also when `state.pushChecked` is false — `refreshAll()` runs before the service worker is ready, so without that flag the not-yet-known empty fingerprint reads as a mismatch and flashes a false warning on every launch. And **`copyPushSub()` no longer clears anything** — the warning lifts only when CI records the new fingerprint, which is the first moment anyone actually knows the secret was updated.

**The 다시 구독 button is now two-tap (`state.pushConfirm`), because pressing it on a healthy subscription is itself an outage.** It calls `unsubscribe()` + `subscribe()`, which kills the endpoint sitting in the secret; every send 410s until the secret is updated. It is offered unprompted only in the `pushDead` state; otherwise it lives inside the 구독 정보 panel behind a confirm that spells out the consequence.

**A given breakage produces exactly one failed-workflow email.** `send-push.js` still exits 1 on the first 404/410, so GitHub mails and the failure lands in the app's own 메일 card — a silent green checkmark cost half a day on 2026-08-10. But it exits 0 on repeats of a breakage already recorded in `push/state.json` for the same fingerprint: the alerts workflow fires up to ~30×/day and unconditional failure buried one real signal under dozens of identical mails on 2026-08-12~13. Recovery (201) followed by a *new* breakage alerts again, because the recorded status changed in between. Both workflows run `node scripts/push-state.js` unconditionally before their commit step, so a secret update is reflected within ~30 minutes during market hours rather than at the next send; the send result is committed by a separate `if: always()` step, since the 410 that fails the job is precisely the record the app needs. `record()` rewrites the file only when fingerprint or status changes, so those 30-minute runs don't accumulate timestamp-only commits — which is why `at` means "when this state began", not "last checked".

`sw.js` also has a **`pushsubscriptionchange`** handler now, for the case where the browser rotates the subscription on its own — without it the subscription simply disappears and the server finds out via 410. It re-subscribes and tells nobody, deliberately: there is no server to notify, and the fingerprint comparison above surfaces it on the next app open. Note that this handler is itself a silent-rotation source — it restores *a* subscription, not *the* subscription in the secret — which is another reason the comparison has to come from the server side rather than from anything the device remembers. It takes the key from `event.oldSubscription.options.applicationServerKey` when present and otherwise regexes `VAPID_PUBLIC_KEY` out of `index.html` (the same single-source trick `send-push.js` uses) rather than hardcoding a second copy of the key in `sw.js`.

**Five outages in six days, and the cause of each was guessed rather than known — so 2026-08-14 added the two things that make the next one readable.** That day's state: the subscription minted 08-13 11:55 KST sent once successfully (201), was dead by 16:43 KST the same day, and the phone was left holding **no subscription at all** (`getSubscription()` → null, which renders the `!state.pushSub` + `pushStale` branch, not `pushDead`). Nothing in this repo can do that — `sw.js`'s push handler always calls `showNotification` (so no `userVisibleOnly` budget violation), and nothing auto-unsubscribes. That leaves browser/OS causes, and they are indistinguishable after the fact, which is the actual problem. Two additions:
- **`navigator.storage.persist()`** (`ensurePersist()`, called at startup and again from `enablePush`/`resubscribePush` where a user gesture makes approval most likely). Without persistence the browser may evict the whole origin under storage pressure — service worker registration, push subscription and `localStorage` together. That fits the evidence better than anything else: it takes the subscription *away* rather than merely killing it, needs no user action, and explains why the `mypush_synced_v1` baseline was found missing on 2026-08-12. The 구독 정보 panel now prints 저장소 영구 보관: 예/아니오, so the answer is visible instead of assumed. On the phone it reads **예** as of 2026-08-14 — but read that carefully: `ensurePersist()` *requests* persistence before reporting it, so a 예 on the first run after this shipped says the origin is protected **from now on**, not that it was protected during the five outages. The eviction hypothesis is therefore neither confirmed nor ruled out; what settles it is whether deaths continue now that the guard is in place, and the subscription-change log is what will say which kind of death it was.
- **A subscription-change log** (`mypush_log_v1`, last 12 entries, rendered in the same panel). `refreshPushState()` appends a line whenever the observed endpoint fingerprint differs from the last one recorded, and `enablePush`/`resubscribePush` label their own. `sw.js`'s `pushsubscriptionchange` can't reach `localStorage`, so it leaves a fingerprint in a **separate `push-diag-v1` cache** — which `activate` must keep excluding from its cache sweep, or the evidence is deleted on the next deploy — and `drainSwPushMarks()` folds it in on the next app open. Read it like this: a `브라우저가 구독 교체` line means the browser rotated it on its own; a changed fingerprint with *no* such line means the registration was replaced (reinstall/data clear); an **empty log next to a live subscription means the log itself was wiped**, which is eviction or a reinstall. `logPush` normalizes an empty fingerprint to `(없음)` before its dedupe check — comparing the raw `''` against the stored `(없음)` would append an identical line on every single app open.

**So the subscription moved out of GitHub Secrets entirely (2026-08-14).** Five deaths in six days made the conclusion unavoidable: a value the browser rotates on its own cannot live somewhere only a human can update. `worker/push-store.js` is a **second, separate Cloudflare Worker** (`push-store.ssangku2.workers.dev`) — deliberately *not* an addition to `stock-proxy`, whose `?symbol=`/`?url=`/`?news=` contract the portfolio app depends on and which isn't in this repo. It stores exactly one subscription in a KV namespace (`PUSH_KV`, key `current`) behind `GET /sub` (read, `ADMIN_TOKEN`) and `POST /sub` (write, `WRITE_TOKEN`). Like `stock-proxy` it is deployed by hand from the Cloudflare dashboard, so **the file here and the deployed script must be changed together**. It went live the same day and the whole path was verified end to end: the phone registered itself at 13:29 UTC, and the next run logged `구독 출처: 워커` → `푸시 발송 완료 (201)` with `push/state.json` picking up the new fingerprint (`fc27bbf4af0f842a`) on its own — no secret was touched.
- **Dashboard trap that costs ten minutes: saving variables does not deploy them.** Adding `WRITE_TOKEN`/`ADMIN_TOKEN` under Settings → Variables and secrets creates a *new version* (`Add secret: …`) and leaves the previous one as the active deployment, so every request keeps answering 401 with what look like correct tokens. Deployments → the new version's ⋯ → **Promote version** is what applies it. Adding the KV binding, by contrast, deploys immediately. When the worker 401s on a token you just entered, check the active version ID before suspecting a typo.
- **The write token is public and that is not a bug.** It ships inside `index.html`, which is committed and served over Pages; a browser cannot hold a secret. What it buys is that a passing scanner can't overwrite the record. The *read* side is what actually matters and is separately gated, because the endpoint is the address that reaches the phone. Nothing leaks through a bad write either — pushes carry no payload, the text is assembled on the device from public Pages JSON — so the worst case is "notifications stop", which is precisely the failure the whole change exists to end. Writes are also restricted to known push-service hosts (`fcm.googleapis.com`, Mozilla, Apple, Edge), without which a stored junk endpoint would have CI POSTing at an arbitrary server every 30 minutes.
- **The app registers itself, in three places, so no path leaves the server behind**: `enablePush`/`resubscribePush` (forced), `refreshPushState` (only when `push/state.json`'s fingerprint differs from this device's — i.e. the very state that used to require a human), and **`sw.js`'s `pushsubscriptionchange`**, which matters most because it fires when nobody is looking. That handler now always fetches `index.html` (it needs both `VAPID_PUBLIC_KEY` and the store URL/token out of it) instead of skipping the fetch when `oldSubscription` carries a key. `syncPushSub` posts the subscription JSON as a plain string so the request stays CORS-simple — **do not add headers or `Content-Type: application/json`**, that turns it into a preflighted request for no gain — and a `mypush_store_v1` timestamp keeps it from re-posting the same fingerprint during the ≤30 min before CI records it.
- **`PUSH_SUBSCRIPTION` still works and is still the fallback.** `scripts/push-store.js`'s `resolveSubscription()` prefers the worker and falls back to the secret whenever `PUSH_STORE_URL`/`PUSH_STORE_ADMIN_TOKEN` are unset or the worker is unreachable, so the pipeline behaves exactly as before until the worker is deployed. Both `send-push.js` and `push-state.js` go through it — **they must resolve the same subscription**, or the app compares its own endpoint against a fingerprint for a subscription nothing actually sends to. That is why `endpointHashFromEnv()` became async and no longer reads the env var directly.
- What this leaves for a human: tapping 알림 받기 once if the subscription is gone entirely. Everything after that is automatic, and the app says so — the 어긋남 warning is now `⏳ 새 구독 등록됨 — 30분 안에 반영` when the store accepted the registration, and only falls back to the old "paste it into the secret" wording when the store write itself failed.

**A subscription nobody has sent to is not a working subscription — hence the verification send (2026-08-15).** The day after the worker store went live, the morning notification was gone again, and the failure was one the store itself had made possible. Timeline (KST): 22:29:37 the phone registered `fc27bbf4`, 22:31:19 a manual test send returned **201**, 22:33:39 the store's record was replaced by `eeece3af`, 23:14:50 CI picked up that new fingerprint with `status: 0`, and the first actual send to it — 00:23:27, nine hours later — got a 410. **The subscription that was verified and the subscription that was stored were never the same one.** Automatic re-registration removed the human from the loop but put nothing in their place: a fingerprint could change at any hour and sit unsent-to until the next alert or the next morning. So `push-state.js` now emits `verify=true` when it records a subscription that has never been sent to, and both workflows answer it with a send right there — the failure surfaces within ~30 minutes during market hours instead of at the next briefing. Details that matter:
- **The trigger is `status === 0`, not "the fingerprint changed."** 0 means "never attempted," and `send-push.js` now records *something* for every attempt, including a new `-1` for "request threw before any response" (timeout/DNS). Keying on fingerprint changes instead would leave a subscription permanently unverified whenever that one verification send happened to fail on the network; keying on `status === 0` while letting a throw go unrecorded would re-fire the check — and a notification — every 30 minutes. Both halves are needed. `-1` is deliberately not one of the statuses `updatePushStale()` warns about: it says the send didn't get through, not that the subscription is gone.
- **The workflows skip it when a real send already ran** (`alerted`/`generated`) — that send is itself the verification. The alerts workflow folds this into the old manual `test_push` step, since both do the same thing for the same reason.
- **Verified in production the same day it shipped.** The phone re-subscribed at 04:39:24Z; the next alerts run logged `아직 발송해 본 적 없는 구독입니다` for `f7f810cd54eedd52`, committed the fingerprint, waited, sent, and recorded `푸시 발송 완료 (201)` at 05:23:23Z — about 44 minutes from re-subscribe to confirmed-alive, and the notification arrived on the phone. Under the old arrangement that confirmation would not have existed until the next briefing.
- **`sw.js` changes the title, not the body.** `status === 0` in `push/state.json` means the push now arriving *is* that first send, so the notification reads 🔔 알림 다시 연결됨 under tag `push-verify`. The body still carries the briefing, because on a morning when the subscription rotated overnight the briefing send and the verification send are the same push — swapping the body out would cost the day's briefing. Reading a stale `state.json` (Pages hasn't deployed yet) just yields the ordinary notification, so the guess is free; the verification steps still `sleep 90` first to make it usually correct.

**What the device log then ruled out (2026-08-15).** The first reading of `mypush_log_v1` after it shipped:

```
8/14 22:32  구독이 사라짐 (없음)
8/14 22:33  직접 알림 켜기      eeece3af
8/14 22:33  서버에 구독 등록    eeece3af
8/15 07:06  구독이 사라짐 (없음)
```

The subscriptions are not being *killed*, they are **disappearing** — `getSubscription()` returns null — and each one lasted under two hours. Four hypotheses die on this log:
- **Not browser key rotation.** A `pushsubscriptionchange` would have left a 브라우저가 구독 교체 line via the `push-diag-v1` cache. There is none, either time.
- **Not `unsubscribe()` from this app.** 다시 구독 writes its own line; 22:33 is 직접 알림 켜기, i.e. `enablePush` — the app was already showing the 알림 받기 button, so the subscription was gone *before* the user touched anything.
- **Not site-data clearing or a reinstall.** Those take `localStorage` with them, and the log itself survived across both events and overnight.
- **Not storage eviction.** `navigator.storage.persisted()` read 예 on 2026-08-14, and 07:06 happened anyway. That was the leading hypothesis when `ensurePersist()` was added; it is now falsified rather than merely unconfirmed.

What remains is the registration or the permission being revoked from outside the app — most plausibly Android reclaiming notification permission for an app it considers unused, which discards the subscription with it. `logPush` has recorded `Notification.permission` on every line since 2026-08-14 but `renderPushDiag` was not printing it; it now appends `· 권한 <값>` whenever the value is not `granted`. **A 구독이 사라짐 line carrying `default` settles this** — permission was taken away. One still carrying `granted` rules permission out too, and points at the service worker registration alone being dropped.

**So Android's auto-revoke was turned off on the phone (Samsung, 2026-08-15), and that is now a fixed condition of the experiment.** It was the leading hypothesis and it is no longer in play, which makes the next disappearance more informative than the last five: if subscriptions keep vanishing with `권한 granted` on the line, permission reclamation is ruled out along with rotation, user action, data clearing, and eviction — and the remaining suspect is the service worker registration being dropped on its own (a WebAPK update from the 2026-08-11 icon change is the one known candidate, since the reinstall it triggers is invisible from inside the app). If they stop vanishing, the setting was the answer. Either way the verification send now bounds the damage to ~30 minutes of market hours rather than a whole morning, so this can be observed rather than chased.

**The auto-revoke setting did not stop it, and 2026-08-17 is the first outage with server-side timestamps precise enough to name the two separate failures.** The CI record (`push/state.json` history + Actions logs) reads: `f7f810cd` verified 201 on Sat 2026-08-15 05:23Z, then **nothing sent for 40 hours**, then 410 on Mon 08-16 21:44Z (the 08-17 briefing). The device log says it was already gone by 08-16 09:04 KST **carrying `권한 default`** — so permission reclamation is *not* ruled out; it happened again two days after the phone setting was changed. Then the recovery attempt failed too: the user tapped 알림 받기 at 08-17 07:42 KST, the store accepted `cefbbee5` at 22:42:32Z, and the verification send **410'd 17 minutes later** at 22:59:39Z, with the device holding no subscription at all by 08:53 KST. A subscription that dies 17 minutes after a hand-granted permission is not slow decay, and the two failures need different answers:
- **The 40-hour hole was structural, not bad luck.** `alerts.yml` is `* * 1-5` and `generate.js` skips Sat/Sun, so between Friday's last alert and Monday's briefing *nothing ever sends*, and a send is the only way the server learns a subscription is dead. `push-state.js` ran all weekend and stayed silent because it only recorded — its `verify=true` fires on `status === 0`, and a subscription that dies while sitting at 201 never reaches 0. Fixed by giving the file a **`sentAt`** ("last send *attempted*", updated on every send even when the status is unchanged — unlike `at`, which is "when this state began" and stays put for weeks on a healthy subscription, making it useless for this). `verify=true` now also fires when `sentAt` is older than **20 hours**, and `daily.yml` runs seven days a week, so the blind window is at most one day. 20h is chosen to sit under a 24h cycle without ever doubling up with the weekday briefing send, which refreshes `sentAt` at ~06:44 KST. The visible cost is one extra notification on Saturday and Sunday mornings (it carries the agenda, since no briefing file exists those days) — that is the price of not discovering a dead subscription on Monday.
- **Recovery no longer requires a human noticing a warning.** `autoHealPush()` (called from `refreshPushState`, which `refreshAll` now runs on *every* refresh rather than only at startup — an installed PWA lives for days, and checking once at launch meant a mid-life death went unseen) re-subscribes silently when `Notification.permission` is still `granted` and either the subscription is gone or the server recorded 410/404 for this exact fingerprint. **This is only safe because of the 2026-08-14 worker store**: before it, minting an endpoint meant notifications stayed dead until a human pasted a secret, which is exactly why 다시 구독 was made two-tap. It never touches a healthy subscription, and a 10-minute throttle (`state.pushHealAt`, a timestamp rather than a once-per-load flag, so a second death in the same long-lived session is still healed) keeps a failing `subscribe()` from retrying on every foreground.
- **What is still manual is the permission grant**, because `Notification.requestPermission()` needs a user gesture. So the `!pushSub` branch now distinguishes "never set up" from "the phone took the permission away" (permission ≠ granted *and* the log shows a subscription once existed) and says so, with the Samsung setting path in the message — a silent 알림 받기 button was being read as "it turned itself off again" rather than as something to fix in phone settings.
- **The log ambiguity that cost a full analysis round is gone.** `renderPushDiag` printed `권한` only when it wasn't `granted`, so a bare 구독이 사라짐 line was indistinguishable from an omitted one; it now prints the permission on **every** line, plus the server's last send result. `logPush` takes an optional permission argument so `sw.js`'s marks carry the value from when the event fired (via `pushManager.permissionState()`, since a service worker has no `Notification.permission`) instead of from when the app next opened. And `navigator.permissions.query({name:'notifications'}).onchange` now logs revocation *as it happens* while the app is open, which is the one piece of evidence no post-hoc reading can reconstruct.

**The cause of all eight outages, settled 2026-08-17: the origin's notification permission belongs to a *different* PWA.** GitHub Pages puts every one of this user's apps on one origin — `ssangku2-tech.github.io/daily-brief/`, `/portfolio/`, and a third app, `One More`. Notification permission is per **origin**, not per path, and when Android installs a PWA as a WebAPK, Chrome delegates that origin's notifications to that app. With more than one WebAPK on the origin, exactly one owns the delegation: Chrome's site settings for `ssangku2-tech.github.io` read **"One More 앱에서 관리함"**. So My Agent's web notification permission is really *One More's Android app notification permission*, and every setting applied to the My Agent app entry (auto-revoke off, sleeping-apps exclusion) was locking a door that was never the entrance. When Android reclaimed the rarely-*launched* One More WebAPK's permission — app launches are what Android counts, not visits to the site — My Agent's permission became `default` and its push subscription was discarded with it. That is the whole eight-outage sequence, and it needs no per-incident explanation: push notifications were built on 2026-08-05 and the delegation predates them by two months, so there was never a healthy baseline to explain away.

Confirmed live rather than argued: at 2026-08-17 16:16 KST the user toggled One More's notification permission back on, and the device log recorded, in the same minute, `알림 권한 → granted` → `자동 재구독(사라짐)` → `서버에 구독 등록`, with CI answering 201 two minutes later. The permission flipping the instant a *different app's* switch was touched is the delegation, observed directly. It was also the first outage to recover with nobody tapping anything in this app — `watchPushPermission` caught the change, cleared the heal throttle, and `autoHealPush` did the rest.

Two consequences worth keeping. **The fix lives in the other app**: One More needs auto-revoke off, notifications on, and exclusion from battery sleep; nothing done here can substitute. If it recurs, the structural fix is moving **One More** to a different origin (Cloudflare Pages costs nothing) — never this app, because `daily-brief` and `portfolio` must share an origin for the 자산 card to read `portfolio_v2` and `app_pin_hash_v1`. And **reinstalling any PWA on this origin can move the delegation again**, so the 자산 card's origin-sharing trick and the push subscription's fragility are two faces of one fact. The permission line in the log is what makes this readable: `구독이 사라짐 · 권한 default` means the delegate app lost its permission, `권한 granted` means something dropped the service worker registration instead — a genuinely different investigation. (That line is also why `refreshPushState` suppresses a repeated 구독이 사라짐 by comparing the previous entry's *kind* rather than its fingerprint: permission lines carry an empty fingerprint, and the old fingerprint-based check swallowed the disappearance that followed one.)

**A ninth outage the next night (2026-08-18) — the first one this repo recovered from with nobody doing anything, and the one that falsifies the "rarely launched" half of the explanation above.** Server-side timeline, all KST, from `push/state.json` history plus the Actions logs' `구독 출처: 워커(등록 …)` line (that line is what dates a *registration*; `at`/`sentAt` only date what CI did with it):

| 시각 | 지문 | 사건 |
|---|---|---|
| 08-17 16:16:43 | `3828daf5` | One More 권한 복구 직후 자동 재구독 → 16:18:52 확인 발송 201 |
| 08-17 20:15:30 | `73c26d17` | 폰이 또 새 구독 등록 = 위 구독이 ~4시간 만에 소멸 |
| 08-17 22:39:53 | `73c26d17` | CI 첫 확인 발송 → **410** (등록 2시간 24분 안에 이미 죽음) |
| 08-18 00:24 / 01:27 | `73c26d17` | 급변동 알림 2건 발송 시도 → 410, 사용자는 못 받음 |
| 08-18 06:34:48 | `e9746e34` | 앱을 연 순간 `autoHealPush`가 조용히 재구독 → 06:50:56 확인 발송 201 |

Registration to confirmed-alive was 16 minutes, and the morning briefing still arrived (as 🔔 알림 다시 연결됨, since `status === 0` at delivery). The only real loss was the two overnight alerts. Under the pre-08-17 arrangement this would have been another silent lost morning, so the 2026-08-17 auto-heal + 20-hour `sentAt` work is doing exactly what it was built for — **the remaining problem is frequency, not recovery.**

The device log for 06:34 read two lines in the same minute — `구독이 사라짐 · 권한 default` then `구독이 사라짐 · 권한 granted` (with an `알림 권한 → granted` line between them, which is what lifts `refreshPushState`'s kind-based dedupe). So the permission was **already `default` when the app opened and returned to `granted` by itself**, and that is the fact that reshapes the diagnosis:
- **`권한 default` confirms the delegation is still the mechanism** — the subscription died with One More's permission, exactly as the paragraph above says.
- **But "Android reclaimed the permission of a rarely-*launched* WebAPK" is wrong, and should not be repeated.** The user opens One More every day, from its home-screen icon (i.e. the WebAPK itself, which is what Android counts). Android's unused-app auto-revoke also never restores a permission on its own — the user has to re-grant it — yet here it came back untouched. A permission that reads `default` and then `granted` again is a delegate app that was **asleep or force-stopped**, not one that was stripped. The 2~6-hour death interval fits that and does not fit auto-revoke's multi-day cycle.
- What was actually still unset on the phone: 절전 앱/심층 절전 앱 목록에는 One More가 이미 없었고, `설정 → 애플리케이션 → One More → 배터리`만 `제한 없음`이 아니었다. That one setting was changed on 08-18 and is the only variable in play, deliberately — nothing else was touched so the next 24 hours are a clean test. If deaths stop, battery restriction was it. If they continue, the remaining suspect is **WebAPK update / delegation hand-off** (Chrome silently reinstalls a WebAPK when its manifest changes, and this origin has three of them), which no phone setting can fix and which makes moving One More to its own origin the answer.

**Never turn One More's notification permission off.** The user's instinct on 08-18 was that they don't need alarms from that app anymore — but under the delegation, One More's Android permission *is* this app's web permission, so switching it off kills the briefing and alert pushes instantly (the 16:16 recovery, run in reverse). Turning the alarm off *inside* One More is fine; the Android permission toggle must stay on. Also worth recording as the origin of the whole mess: that One More alarm feature is what obtained the permission in the first place, which is how the delegation landed there two months before push notifications existed here.

**The delegation moved again, and 2026-08-19 is the first outage where the delegate app itself was checked rather than assumed.** Chrome's site settings for `ssangku2-tech.github.io` now read **"My Agent 앱에서 관리함"** — not One More. Delegation is decided by Android/Chrome (evidently by something like most-recently-installed-or-launched WebAPK), not fixed to whichever app it favored last, so any past instruction to fix settings "on the delegate app" has to be re-checked against current site settings each time, not assumed to still mean One More. Two deaths that morning (`구독이 사라짐 · (없음) · 권한 granted` at 07:33 and again at 13:13 KST, each auto-healed within the same log line group) both carry `권한 granted` — by the rule established 2026-08-15, a `granted` line means permission reclamation is *not* the cause and the service worker registration itself was dropped instead, which is consistent with My Agent (not One More) now being the app Android's battery management actually governs for this origin. The user's My Agent app had **not** had its battery optimization set to unrestricted until this same morning (it had only ever been done for One More, back when One More was believed to be the delegate); it was set to unrestricted immediately after noticing the second death. Both deaths self-healed via `autoHealPush` with no user action beyond opening the app, and the resulting subscription (`103038ce…`) verified 201 at 2026-08-19T04:37:58Z. This does not settle battery management as the cause — two data points, and the auto-revoke-off setting looked settled after one clean interval too (2026-08-15) before failing again — but it is now the live hypothesis, and it is at least pointed at the *correct* app for the first time. **If subscriptions keep dying with `권한 granted` from here, check Chrome's site-settings delegation line again before re-diagnosing anything else** — it may have moved a third time, and every battery/auto-revoke fix applied to the wrong app is invisible from inside this one.

**첫 무사한 하룻밤: 2026-08-20 아침 브리핑이 도착했다 — 8/19 위임 앱 교정 이후 첫 데이터 포인트.** `push/state.json` 이력 기준(KST): `103038ce311ae1ff`가 08-19 13:36에 등록되고 13:37에 확인 발송 201, 그 뒤 발송 시도 없이 밤을 넘겨 **08-20 06:51 브리핑 발송이 201**로 돌아왔고 폰에도 실제로 알림이 떴다. 살아 있던 시간이 최소 17시간 15분으로, 8차 끊김 이후 처음으로 밤을 넘긴 구독이다 — 직전 기록은 `e9746e34` 약 8시간, `3828daf5` 약 4시간, `73c26d17` 2시간 24분 미만, `0e9dc69323016b20`(08-19 07:36 등록)은 **90초**였다. 08-19 아침 브리핑(08-19 06:48 발송)은 410으로 못 받았으므로, 오늘이 08-18 이후 처음 도착한 아침 알림이기도 하다. **연속 생존은 서버 기록만으로는 확정할 수 없었고, 기기 로그가 그걸 마감했다.** 13:37과 06:51 사이에는 발송 시도가 아예 없어(20시간 `sentAt` 규칙에 17시간은 못 미친다) 서버가 아는 것은 양 끝뿐이었다 — 그 사이 죽었다 `autoHealPush`로 되살아난 경우도 서버 기록에서는 똑같이 보인다. 08-20에 폰의 `mypush_log_v1`을 확인한 결과 그 구간에 `구독이 사라짐` 줄이 **없었고**, 그래서 17시간 15분이 진짜 연속 생존으로 확정됐다. 앞으로도 "밤을 넘겼다"를 주장하려면 이 두 가지를 같이 봐야 한다. 다만 이건 여전히 **한 번의 밤**이다.

같은 날 크롬 사이트 설정의 위임 줄도 다시 읽었다 — 여전히 **"My Agent 앱에서 관리함"**으로 8/19 이후 옮겨가지 않았다. 이걸 확인해야 하는 이유는 위임 이동이 배터리 가설의 경쟁 가설이기 때문이다: 위임이 다른 앱으로 넘어가 있었다면 이번 무사한 밤은 설정 덕분이 아니라 조건이 바뀐 결과였을 수 있다. 같은 위임 대상에서 나온 결과이므로 8/19 설정과 이번 밤은 같은 실험의 연속이다. (알림 목록에는 `google.com`도 같은 방식으로 구글 앱에 위임돼 있는데, 이 앱과는 무관하다 — 위임 자체가 이 오리진만의 이상 현상은 아니라는 뜻이기도 하다.) 08-15에도 한 번 깨끗한 구간 뒤에 다시 죽었으니, My Agent 배터리 제한 없음이 원인이었다고 결론 내리기에는 이르다. 다만 8/19에 처음으로 *올바른 앱*에 설정을 건 뒤 나온 결과라는 점에서, 그 가설을 지지하는 첫 증거다.

Testable without Cloudflare, and was: the worker module runs under Node if `export default` is swapped for `module.exports` and `crypto`/`Response`/`URL` are passed in, with KV stubbed as a plain object — that covers the 401/404/400 paths, host filtering, and the no-rewrite-on-identical-endpoint case. Point `PUSH_STORE_URL` at that local server and `send-push.js` runs end to end against an `http.createServer` standing in for the push service (the endpoint host check lives in the worker, so an unroutable `127.0.0.1` endpoint has to be injected into the KV stub directly). On the client, `page.addInitScript` can wrap `window.fetch` to redirect the `workers.dev` URL to a local server — `PUSH_STORE_URL` is a `const` in script scope and can't be reassigned from `evaluate`.

Testing note: headless Chromium reports `Notification.permission === 'denied'` even after Playwright's `grantPermissions(['notifications'])`, and cannot reach a real push service, so the subscribe path can only be exercised on the actual phone. What *is* testable locally, and was: the VAPID signature (verify with the public key, check the 64-byte P1363 length), the `sw.js` body-building across all four data-availability cases (run the handler in Node with stubbed `self`/`fetch`), the `pushsubscriptionchange` handler the same way (assert it skips the `index.html` fetch when `oldSubscription` carries a key, produces a 65-byte `0x04`-prefixed key when it doesn't, and swallows a fetch failure), and each `renderPushRow()` branch. The render branches are cheaper to test by extracting the functions out of `index.html` and `eval`-ing them in Node against a stubbed `state`/`localStorage` than by driving a browser — but note that **Node 18+ has a read-only built-in `navigator` global**, so `global.navigator = {serviceWorker:{}}` is silently ignored and `pushSupported()` returns false, making every branch render an empty string. Use `Object.defineProperty(global,'navigator',{value:…})`, and stub `window` too (`pushSupported` reads `'PushManager' in window`). `crypto.subtle` is a real global in Node, so `pushFingerprint()` runs unmodified and can be asserted equal to Node's `createHash('sha256')` — that equality is what makes the client/CI comparison work at all. To drive those branches in a *real* page instead (worth it when the change touches CSS or the panel layout), Playwright's `addInitScript` can redefine `Notification.permission`'s getter to `'granted'`, after which `page.evaluate` can set `state` and call `renderPushRow()` directly — bare names, not `window.state`, since the script scope isn't on `window`.

`send-push.js`'s exit-code logic is testable end to end without a push service: stand up an `http.createServer` that returns a scriptable status, generate a throwaway P-256 keypair and feed its JWK `x`/`y`/`d` in as `VAPID_PUBLIC_KEY`/`VAPID_PRIVATE_KEY`, and point `PUSH_SUBSCRIPTION` at `http://127.0.0.1:<port>/…`. Drive it through 201 → 410 → 410 → 500 → 201 → 410 and assert the exit codes are `0,1,0,0,0,1`. **Use async `spawn`, not `spawnSync`** — `spawnSync` blocks the parent's event loop, so the in-process test server never answers and every case fails with a 15-second fetch timeout that looks like a network bug.
- All five cards are real now: 미국 시장, 일정, 메일, 오늘의 학습 (localStorage check-in), 내 자산. Card order in `render()` is 미국 시장 → 일정 → 메일 → 학습 → 자산.
- **자산 card** (2026-08-05): holdings come from the companion portfolio app **for free, because the two PWAs share an origin**. `https://ssangku2-tech.github.io/portfolio/` and `/daily-brief/` differ only by path, and `localStorage` is scoped per *origin*, so `portfolioHoldings()` simply reads the other app's `portfolio_v2` key — no export, no sync service, no manual entry. **It shows only the portfolio app's 종합 tab scope, not every account** (2026-08-06): that tab's `aggregateAllStocks()` sums `mirae1` + `mirae2` and deliberately excludes `mirae3`/`samsung`, so `AGG_ACCOUNTS` here lists the same two account keys and merges a symbol held in both into one row with a quantity-weighted average cost — same arithmetic (`totalCost / qty`, lots with no `avgPrice` counted as 0) as that tab. A row therefore carries no account name, since a merged row has more than one. Changing which accounts the 종합 tab aggregates means changing `AGG_ACCOUNTS` too. **The 급변동 알림 export is deliberately not narrowed with it** (2026-08-06): `portfolioHoldings(accounts)` takes the account list as an argument, `effectiveHoldings()` passes `AGG_ACCOUNTS` (what the card draws) and `alertHoldings()` passes `null` (every account). The two answer different questions — the card is "what am I watching this morning", the alert list is "tell me if any stock I own moves hard" — and there is no cost argument for narrowing the latter, since the alert path is Worker quotes only, no LLM. Consequences to keep intact: the export button prints its own count (`전 계좌 N종목`) because it will differ from the card's `종합 N종목`, and it renders *outside* the `holdings.length` branch so it still appears when the 종합 tab is empty but another account isn't. It reads `accounts.<acct>.stocks[]` (`{id, name, code, qty, avgPrice, currentPrice}`) and reuses that app's symbol rule (`/^\d{6}$/` → `code + '.KS'`, else the ticker as-is) plus the same Worker, so Korean and US holdings both price correctly. **This depends on another repo's internal storage format** — if the portfolio app changes its schema or key, this breaks silently and both must be updated together. `effectiveHoldings()` falls back to the app's own `myassets_v1` manual list when no portfolio data exists on the device (a different browser, or the portfolio app never opened here), and the manual add-form only renders in that fallback case.
- **Amounts are masked by default** in the 자산 card, and the mask state lives in `state.assetsRevealed` — deliberately *not* in `localStorage`, so reopening the app re-hides them. **Unmasking requires the portfolio app's PIN** (2026-08-06): `toggleAssets()` reads that app's `app_pin_hash_v1` (same origin, so it's simply there) and compares against `pinHash()`, which **must stay byte-identical to the portfolio app's `simpleHash`** — it is not a hash this repo chose, it's the one that produced the stored value. It is not cryptographic there either; both apps are only deterring a casual over-the-shoulder look. Three behaviors are deliberate: 가리기 does **not** ask for the PIN (only revealing does); revealing asks **every time**, with no "unlocked for this session" flag, because an installed PWA can stay alive for days and a session flag would leave it effectively unlocked; and if `app_pin_hash_v1` is absent (portfolio app never opened on this device, so there's no stored hash to compare against) the reveal just happens — inventing a PIN here would lock the user out of their own numbers. The typed digits live in `state.pinInput`, and `onPinInput()` skips `render()` until the 4th digit precisely because `render()` rewrites the card's `innerHTML` and would drop input focus after every keystroke. Masking covers the value column, both P/L amounts (누적 **and** 당일), **and the share quantity** — leaving quantity visible next to the (unmasked) price would let anyone multiply them back, and 당일 손익 next to 전일 대비 % gives it away the same way. Percentages stay visible on purpose: they answer "how am I doing" without exposing net worth. Never put an amount in the card's header `sub`, which shows even when the card is collapsed.
- **Rows are ordered by evaluated value, descending** (2026-08-06): `renderAssets()` sorts a `slice()` of the holdings — never the array itself, since `effectiveHoldings()`'s result is also what `exportAlertSymbols()` walks — using a KRW-normalized key (`price × qty × fx` for USD), because comparing raw numbers would rank a $200 position above a ₩200,000 one. Holdings whose quote didn't arrive sort to the bottom (key `-1`), which is also where they belong visually since they render a shorter row. When `fx` is null the key degrades to raw numbers and KRW holdings bunch at the top; that is the same situation in which the 총 평가액 block already splits by currency, so it's visible rather than silent.
- **A holding row is three columns, and its height is the constraint** (2026-08-06): 이름/수량 · 현재가/전일 대비 %/당일 손익 · 평가액/누적 손익/누적 %. The middle column was added by using the row's empty *horizontal* space, explicitly not by stacking more lines, so `.asset-name` is `nowrap` + ellipsis (a wrapping name would grow the row) and the middle column caps at 3 lines to match the right one. Holdings with **no** `avg` are the exception worth preserving: their right column is one line, so 당일 손익 moves *there* and the middle stops at 2 lines — otherwise that row alone would grow ~15px. Verified by measuring row heights before and after at 360px and 390px (77px with `avg`, 58px without, unchanged both times); redo that measurement if you add anything to this row. 당일 손익 is `(price − prev) × qty` off the Worker's own `prev`; the portfolio app instead uses the buy price as the base for a lot bought *that day*, and this app doesn't read that flag, so the two can differ on a day you bought.
- Older holdings note: `myassets_v1` (`[{ticker, qty, avg}]`) is **never committed** — this is a public repo, so holdings must stay on the device. Quotes come from the same `loadQuotes()` batch as everything else (holding tickers outside `WATCH` are appended to the symbol list), and `state.live.bySymbol` keeps the *raw* numbers because the formatted `price` strings can't be multiplied. Three things worth not breaking: the total P/L divides `valueCosted` by `cost` (only holdings that have an `avg`) rather than dividing the whole portfolio value by a partial cost basis — the first version did the latter and reported +103% on a +17% position — the 총 평가액 block's **당일 합계** (2026-08-06) sums `dayPl`/`dayBase` per currency in the row loop and divides them at the end, so its percentage is against yesterday's close of *only the symbols that returned a `prev`*, not against the displayed total (a symbol with no `prev` is excluded from both sides rather than counted as flat); `totalBlock(label, t, cur)` takes the whole totals object precisely so adding another aggregate doesn't mean another positional argument, and both P/L lines carry a 오늘/누적 prefix because two unlabeled signed amounts stacked together are unreadable — and the add-form's field values live in `state.assetForm`, because `render()` replaces the card's `innerHTML` wholesale and a DOM-only input would be wiped by any background refresh. The link out to the portfolio app is still there, below the holdings.
- Card `defaultOpen` is content-dependent for 일정/메일 (open only when there is something to show) and always true for 미국 시장; a user toggle still overrides it via `mycard_open_v1`. Each card's header `sub` carries the at-a-glance number (`오늘 2건`, `중요 4건 · 안읽음 201`) so a collapsed card is still informative — don't restate those inside the body.
- **일정·메일 cards, live half (2026-08-07):** `loadGoogleLive()` calls the Calendar and Gmail REST APIs straight from the browser (both send CORS headers) using a **GIS token flow** — `google.accounts.oauth2.initTokenClient` with `calendar.readonly` + `gmail.metadata`, the same minimal read-only scopes as CI. This is deliberately *not* the CI credential path: `GOOGLE_REFRESH_TOKEN` must **never** reach the client, since on a public Pages site it would hand anyone the user's calendar and mail. The token flow needs no client secret and issues no refresh token, so the consent screen's "Testing" 7-day refresh-token cap doesn't apply either.
  - `GOOGLE_WEB_CLIENT_ID` was filled in on 2026-08-07 and the feature is live; **an empty value still hides the whole thing** (both cards fall back to rendering exactly as they did before), which is the switch to flip if it ever needs turning off. The CI OAuth client won't work — it has no JavaScript origin registered; this needs a **Web application** client with `https://ssangku2-tech.github.io` in Authorized JavaScript origins. (Both clients are Web type — the CI one was long recorded here as Desktop, which is wrong; tell them apart by the phone client's ID being the one committed in `index.html`, or by the CI one's `127.0.0.1:53682`/oauthplayground redirects.) A client ID is not a secret (it's locked to that origin), so it's committed.
  - **One token, two cards, one shared status** (`state.googleStatus`), because splitting the scopes would mean two consent prompts. But the *results* are separate (`state.liveEvents` / `state.liveMail`, each fetched under its own `.catch(() => null)`), so one API failing leaves the other live and only the failed half falls back to `agenda/`. `googleConnectRow(what)` draws the same connect/disconnect button in both cards. **The cost of that graceful degradation is that a permanently broken half looks exactly like a stale one**: the Gmail calls 403'd on every load from 2026-08-07 to 2026-08-10 (the `q=` bug above) and the card just kept showing the morning file, so nobody noticed. If a half seems to never go live, read the console rather than trusting the card.
  - `liveEvents` is `{events, at}`, not a bare array — **an empty `events` array is a real answer ("no events"), and must not be confused with "the fetch failed"**. `renderSchedule` branches on the wrapper's existence, never on `events.length`.
  - The mail card is two blocks: **중요 메일** (still the morning `agenda/` file, because the one-line summaries are the Claude call and cannot move client-side — its label says which morning) and **안 읽은 메일** (live subjects/senders, no summaries). The header `sub`'s unread count prefers the live number.
  - **There is no silent token path in GIS, and the code must not pretend there is.** The first version called `requestAccessToken({prompt:'none'})` on every auto-refresh, on the assumption that `'none'` meant "no window". It does not: the token client opens a popup *first* and lets Google decide inside it, so the app popped a window on every launch — and since that popup has no user gesture behind it, a real browser would usually block it outright. Verified headlessly (`page.on('popup')`); this is exactly the kind of thing the branch tests can't catch, so watch for it if this code is touched. The old `gapi.auth2` hidden-iframe refresh does not exist in the token model, and the authorization-code+PKCE alternative is closed off too — Google requires `client_secret` for code exchange on a **Web** client, and that must never ship to the browser.
  - So: **`googleToken(false)` never calls `requestAccessToken` at all** — auto-refresh works only while a stored token is alive, and otherwise throws `needAuth`. `requestAccessToken({prompt:''})` runs only from the button (a real user gesture). The consequence is a ~1 hour live window per tap; the button therefore relabels itself to 실시간으로 다시 불러오기 once `mygmail_on_v1` exists, because the user will see it regularly and "연결" would read as if the setup had been lost. Re-tapping with an existing grant shows no consent screen — the window flashes and closes.
  - The token lives in **`localStorage`, not `sessionStorage`** — an installed PWA gets restarted often, and session storage would throw away a token with 50 minutes left on it. Both cards also keep rendering the last live data they received even after the token expires (`liveOk` is `!!state.liveMail`, not `googleStatus === 'ok'`), since an hour-old live value beats the morning file and the block label states its timestamp.
  - **`googleGet` clears the stored token on 401 but not on 403.** 401 is an expired token and dropping it makes the next refresh get a fresh one; 403 means that scope was never granted, and dropping the token there would re-fetch a token on every single refresh only to get 403 again. `state.googleGrants` (from `hasGrantedAllScopes`) is what turns that case into a visible "권한에 동의하지 않아…" line instead of a silent fallback.
  - Testing note: the real OAuth round-trip can't run headless (same situation as push subscription). What was verified locally is every render branch, by overwriting `state` and calling `render()`: live events (timed + all-day, today + tomorrow, sort order), empty live events, calendar-denied, total failure, need-auth, and loading. Note that `cardShell` omits the body entirely for a collapsed card, so a branch test must force `mycard_open_v1` open or it will silently assert against nothing.
- `briefings/2026-07-30.json` may still contain data from the pre-4섹션 schema (no `sessionDate`/`semiconductorNote`/`aiNews`) — the renderers tolerate this by treating missing fields as empty, but don't expect old files to show the new sections.
- The live quote path has no US-market-hours awareness: during the US session the Worker returns an intraday price, and out of hours it returns the last close, but the UI labels both the same way ("시세 HH:MM 기준"). It also has no auto-polling — the price only moves when the app is opened, foregrounded after 3 minutes, or refreshed by hand.
- 종목별 뉴스(`stocks[].news`) can repeat across days for a quiet stock — see the per-stock news note above; that's the deliberate fallback, not a staleness bug.
